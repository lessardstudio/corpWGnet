# WireGuard Automation System

Система автоматизированной выдачи конфигураций WireGuard через Telegram бот с веб-интерфейсом управления.

## 🏗️ Архитектура

```
┌─────────────┐
│  Telegram   │
│    Users    │
└──────┬──────┘
       │
       ▼
┌─────────────────────────┐
│   Telegram Bot          │
│  (Node.js/TypeScript)   │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────────┐      ┌──────────────────┐
│  Service Manager        │◄────►│   Web Interface  │
│  (Express API)          │      │   (port 3000)    │
└──────┬──────────────────┘      └──────────────────┘
       │
       ▼
┌─────────────────────────┐
│   WGDashboard           │
│   (port 10086)          │
└─────────────────────────┘
```

## 📦 Компоненты

### 1. WGDashboard
- Основная панель управления WireGuard
- API для создания и управления пирами
- Порты: 10086 (HTTP), 51820 (WireGuard UDP)

### 2. Telegram Bot (`tgbot`)
- Выдача конфигураций пользователям
- Генерация QR-кодов
- **Система авторизации** (4 режима: open, whitelist, admin_approval, closed)
- Команды администрирования
- Технологии: Node.js, TypeScript, Grammy

### 3. Service Manager (`service-manager`)
- REST API для управления ссылками
- Генерация одноразовых/многоразовых ссылок
- База данных SQLite для хранения ссылок
- Логирование использования
- Технологии: Express, TypeScript, Better-SQLite3

## 🔐 Система авторизации

Бот поддерживает 4 режима контроля доступа:

### 1. **Open** (Открытый) 🌐
Любой пользователь может получить конфигурацию.
```env
AUTH_MODE=open
```

### 2. **Whitelist** (Белый список) 📋
Доступ только для предварительно одобренных ID.
```env
AUTH_MODE=whitelist
ALLOWED_USER_IDS=123456789,987654321
```

### 3. **Admin Approval** (Одобрение админа) ✋
Пользователи запрашивают доступ, админы одобряют.
```env
AUTH_MODE=admin_approval
```

**Команды для пользователей:**
- `/request_access` - запросить доступ
- `/getconfig` - получить конфиг (после одобрения)

**Команды для админов:**
- `/pending` - список запросов
- `/approve_<user_id>` - одобрить
- `/reject_<user_id>` - отклонить
- `/authstats` - статистика

### 4. **Closed** (Закрытый) 🔒
Только администраторы могут получать конфигурации.
```env
AUTH_MODE=closed
```

📖 **Подробная документация:** [AUTHORIZATION.md](AUTHORIZATION.md)

## 🚀 Быстрый старт

### Предварительные требования

- Docker & Docker Compose
- Telegram Bot Token (от @BotFather)
- VPS/сервер с публичным IP

### 1. Клонирование и настройка

```bash
# Создайте директорию проекта
mkdir wireguard-automation
cd wireguard-automation

# Скопируйте все файлы проекта в эту директорию
```

### 2. Настройка переменных окружения

```bash
# Создайте .env файл из примера
cp .env.example .env

# Отредактируйте .env файл
nano .env
```

Обязательные параметры в `.env`:

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz  # От @BotFather
TELEGRAM_ADMIN_IDS=123456789,987654321                    # Ваш Telegram ID

# WGDashboard
WGDASHBOARD_URL=http://wgdashboard:10086
WGDASHBOARD_API_KEY=your_api_key_here                     # Получить из WGDashboard
WGDASHBOARD_CONFIG_NAME=wg0

# WireGuard
WG_DNS=1.1.1.1
WG_ENDPOINT=YOUR_SERVER_IP:51820                          # Публичный IP вашего сервера
WG_ALLOWED_IPS=0.0.0.0/0

# Service Manager
SERVICE_MANAGER_PORT=3000
LINK_DOMAIN=http://YOUR_SERVER_IP:3000                    # URL для ссылок скачивания
```

### 3. Получение API ключа WGDashboard

```bash
# Запустите только WGDashboard
docker-compose up -d wgdashboard

# Откройте в браузере
http://YOUR_SERVER_IP:10086

# Логин по умолчанию: admin / admin
# После входа:
# 1. Перейдите в Settings
# 2. Прокрутите вниз до API
# 3. Включите API и создайте ключ
# 4. Скопируйте ключ в .env файл
```

### 4. Запуск всех сервисов

```bash
# Сборка и запуск всех контейнеров
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Проверка статуса
docker-compose ps
```

### 5. Проверка работоспособности

```bash
# Проверка WGDashboard
curl http://localhost:10086/api/handshake \
  -H "wg-dashboard-apikey: YOUR_API_KEY"

# Проверка Service Manager
curl http://localhost:3000/health

# Проверка Telegram бота
# Отправьте /start вашему боту в Telegram
```

## 📱 Использование Telegram бота

### Команды для пользователей

- `/start` - Приветствие и информация
- `/getconfig` - Получить новую конфигурацию VPN
- `/help` - Справка
- `/request_access` - Запросить доступ (только для режима admin_approval)

### Команды для администраторов

**Управление пирами:**
- `/stats` - Статистика системы
- `/list` - Список всех пиров
- `/delete <peer_id>` - Удалить пир
- `/restrict <peer_id> <true|false>` - Заблокировать/разблокировать
- `/admin` - Справка по командам администратора

**Управление доступом:**
- `/authstats` - Статистика авторизации
- `/pending` - Список запросов на рассмотрении
- `/approved` - Список одобренных пользователей
- `/approve_<user_id>` - Одобрить пользователя
- `/reject_<user_id>` - Отклонить запрос
- `/revoke_<user_id>` - Отозвать доступ

### Процесс получения конфигурации

1. Пользователь отправляет `/getconfig`
2. Бот создает новый пир в WGDashboard
3. Генерирует QR-код и файл конфигурации
4. Создает временную ссылку для скачивания
5. Отправляет все это пользователю

## 🔧 Service Manager API

### Endpoints

#### Создание ссылки
```bash
POST /api/links
Content-Type: application/json

{
  "peerId": "abc123",
  "expiryHours": 24,
  "maxUsage": 3,
  "userId": 123456789
}
```

#### Получение информации о ссылке
```bash
GET /api/links/:id
```

#### Список активных ссылок
```bash
GET /api/links?userId=123456789
```

#### Деактивация ссылки
```bash
DELETE /api/links/:id
```

#### Скачивание конфигурации
```bash
GET /download/:id
```

## 📊 Мониторинг и логи

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f tgbot
docker-compose logs -f service-manager
docker-compose logs -f wgdashboard

# Логи в файлах
tail -f tgbot/logs/combined.log
tail -f service-manager/logs/combined.log
```

### Health Checks

```bash
# Service Manager
curl http://localhost:3000/health

# WGDashboard
curl http://localhost:10086/api/handshake \
  -H "wg-dashboard-apikey: YOUR_API_KEY"
```

## 🔐 Безопасность

### Рекомендации

1. **Firewall**: Закройте порты 10086 и 3000 от публичного доступа
2. **API Keys**: Используйте сложные API ключи
3. **Telegram Admin IDs**: Правильно настройте admin IDs
4. **SSL/TLS**: Используйте reverse proxy (nginx) с SSL для production
5. **Регулярные обновления**: Обновляйте Docker образы

### Настройка Nginx (опционально)

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## 🛠️ Разработка

### Структура проекта

```
wireguard-automation/
├── docker-compose.yml
├── .env.example
├── .env
├── tgbot/
│   ├── src/
│   │   ├── handlers/
│   │   ├── services/
│   │   ├── types/
│   │   ├── utils/
│   │   └── index.ts
│   ├── package.json
│   ├── tsconfig.json
│   └── Dockerfile
└── service-manager/
    ├── src/
    │   ├── routes/
    │   ├── services/
    │   ├── utils/
    │   └── index.ts
    ├── package.json
    ├── tsconfig.json
    └── Dockerfile
```

### Локальная разработка

```bash
# Telegram Bot
cd tgbot
npm install
npm run dev

# Service Manager
cd service-manager
npm install
npm run dev
```

### Пересборка после изменений

```bash
# Пересборка конкретного сервиса
docker-compose build tgbot
docker-compose up -d tgbot

# Пересборка всех сервисов
docker-compose build
docker-compose up -d
```

## 🐛 Решение проблем

### Бот не отвечает

```bash
# Проверьте логи
docker-compose logs -f tgbot

# Проверьте подключение к WGDashboard
docker-compose exec tgbot sh
curl http://wgdashboard:10086/api/handshake \
  -H "wg-dashboard-apikey: YOUR_API_KEY"
```

### Ошибки при создании пира

```bash
# Проверьте WGDashboard
docker-compose logs -f wgdashboard

# Проверьте доступные IP
# Убедитесь, что в конфигурации есть свободные IP адреса
```

### База данных не создается

```bash
# Проверьте права доступа
ls -la service-manager/data/

# Создайте директорию вручную
mkdir -p service-manager/data
chmod 777 service-manager/data
```

## 📝 TODO / Roadmap

- [ ] Веб-интерфейс для управления (Vue.js)
- [ ] Поддержка нескольких конфигураций WireGuard
- [ ] Автоматическое удаление неактивных пиров
- [ ] Статистика использования трафика
- [ ] Email уведомления
- [ ] Telegram группы поддержки
- [ ] Docker образы в registry
- [ ] Helm charts для Kubernetes

## 📄 Лицензия

MIT

## 🤝 Поддержка

Если возникли вопросы или проблемы:
1. Проверьте логи: `docker-compose logs -f`
2. Убедитесь, что все переменные окружения настроены
3. Проверьте firewall и сетевые настройки

## 🙏 Благодарности

- [WGDashboard](https://github.com/donaldzou/WGDashboard) - отличная панель управления WireGuard
- [Grammy](https://grammy.dev/) - современный Telegram Bot фреймворк
- WireGuard - за отличный VPN протокол
