# WireGuard Automation - Быстрый старт 🚀

## 📦 Что внутри?

Полная система автоматизированной выдачи WireGuard конфигураций через Telegram:

- **Telegram Bot** - выдача конфигов пользователям
- **Service Manager** - API для генерации ссылок
- **WGDashboard** - управление WireGuard пирами

## ⚡ Быстрый старт (5 минут)

### 1. Распаковка

```bash
tar -xzf wireguard-automation.tar.gz
cd wireguard-automation
```

### 2. Настройка .env

```bash
cp .env.example .env
nano .env
```

Минимально нужно настроить:

```env
TELEGRAM_BOT_TOKEN=получить_от_@BotFather
TELEGRAM_ADMIN_IDS=ваш_telegram_id
WG_ENDPOINT=ваш_публичный_ip:51820

# Режим авторизации (опционально)
AUTH_MODE=open  # open, whitelist, admin_approval, closed
```

**Режимы авторизации:**
- `open` - любой может получить конфиг (по умолчанию)
- `whitelist` - только ID из ALLOWED_USER_IDS
- `admin_approval` - пользователи запрашивают, админы одобряют
- `closed` - только админы

📖 Подробнее: [AUTHORIZATION.md](AUTHORIZATION.md)

### 3. Запуск

```bash
chmod +x setup.sh
./setup.sh
```

Скрипт автоматически:
- Проверит Docker
- Создаст необходимые директории
- Запустит WGDashboard для получения API ключа
- Запустит все сервисы

### 4. Получение API ключа

1. Откройте WGDashboard: `http://ваш_ip:10086`
2. Логин: `admin` / `admin`
3. Settings → API → Enable → Create Key
4. Скопируйте ключ в `.env` файл:
   ```env
   WGDASHBOARD_API_KEY=ваш_api_ключ
   ```
5. Перезапустите сервисы:
   ```bash
   docker-compose restart tgbot service-manager
   ```

### 5. Готово!

Отправьте `/start` вашему боту в Telegram.

## 📱 Использование

### Для пользователей

- `/start` - начало работы
- `/getconfig` - получить конфигурацию VPN
- `/help` - справка

### Для администраторов

- `/stats` - статистика
- `/list` - список пиров
- `/delete <id>` - удалить пир
- `/admin` - команды админа

## 📂 Структура проекта

```
wireguard-automation/
├── docker-compose.yml          # Docker конфигурация
├── .env.example                # Пример переменных окружения
├── setup.sh                    # Скрипт установки
├── README.md                   # Полная документация
├── DEPLOYMENT.md               # Руководство по развертыванию
├── API_EXAMPLES.md             # Примеры использования API
├── tgbot/                      # Telegram bot (TypeScript)
│   ├── src/
│   ├── package.json
│   ├── Dockerfile
│   └── tsconfig.json
└── service-manager/            # Service Manager API (TypeScript)
    ├── src/
    ├── package.json
    ├── Dockerfile
    └── tsconfig.json
```

## 🔧 Команды Docker

```bash
# Просмотр логов
docker-compose logs -f

# Перезапуск сервиса
docker-compose restart tgbot

# Остановка всех сервисов
docker-compose down

# Запуск заново
docker-compose up -d
```

## 📖 Дополнительная документация

- **README.md** - полная документация проекта
- **DEPLOYMENT.md** - детальное руководство по развертыванию
- **API_EXAMPLES.md** - примеры использования Service Manager API

## 🆘 Помощь

### Бот не отвечает?

```bash
docker-compose logs tgbot
docker-compose restart tgbot
```

### Ошибка при создании пира?

```bash
docker-compose logs wgdashboard
# Проверьте API ключ в .env
```

### База данных не работает?

```bash
ls -la service-manager/data/
mkdir -p service-manager/data
chmod 777 service-manager/data
docker-compose restart service-manager
```

## 🔐 Важно для Production

1. **Смените пароль** WGDashboard (admin/admin)
2. **Настройте firewall** (закройте порты 10086, 3000 от внешнего мира)
3. **Используйте SSL** (nginx + Let's Encrypt)
4. **Настройте бэкапы** (см. DEPLOYMENT.md)
5. **Мониторинг** логов и ресурсов

## ✨ Возможности

- ✅ Автоматическое создание WireGuard конфигураций
- ✅ QR-коды для быстрого подключения
- ✅ Одноразовые/многоразовые ссылки на скачивание
- ✅ Telegram бот с поддержкой команд
- ✅ Административные команды для управления
- ✅ REST API для интеграций
- ✅ SQLite база данных
- ✅ Логирование всех операций
- ✅ Docker-based развертывание

## 🎯 Roadmap

- [ ] Web UI для администрирования
- [ ] Поддержка множественных конфигураций
- [ ] Статистика использования трафика
- [ ] Email уведомления
- [ ] Kubernetes deployment

## 📝 Лицензия

MIT License - свободно используйте и модифицируйте!

---

**Удачи с развертыванием! 🚀**

Если возникли вопросы - смотрите полную документацию в README.md
