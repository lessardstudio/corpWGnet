# Какой режим авторизации выбрать?

## 🤔 Краткое руководство

### Вопрос 1: Кто будет использовать VPN?

#### ➡️ Только я и несколько коллег (до 10 человек)
**Выбирайте:** `whitelist` или `closed`

```env
# Если только админы
AUTH_MODE=closed
TELEGRAM_ADMIN_IDS=123456789,987654321

# Если несколько известных пользователей
AUTH_MODE=whitelist
TELEGRAM_ADMIN_IDS=123456789
ALLOWED_USER_IDS=123456789,444555666,777888999
```

---

#### ➡️ Сотрудники компании (10-100 человек)
**Выбирайте:** `admin_approval`

```env
AUTH_MODE=admin_approval
TELEGRAM_ADMIN_IDS=123456789,987654321
```

**Почему:**
- Новые сотрудники сами запрашивают доступ
- Админы одобряют одной командой
- Автоматические уведомления
- История всех запросов

---

#### ➡️ Публичный сервис / любой желающий
**Выбирайте:** `open`

```env
AUTH_MODE=open
TELEGRAM_ADMIN_IDS=123456789
```

**Почему:**
- Нулевые барьеры для входа
- Мгновенная выдача конфигураций
- Минимум настроек

---

### Вопрос 2: Как часто появляются новые пользователи?

#### ➡️ Редко (раз в неделю/месяц)
**Выбирайте:** `whitelist`

Вручную добавлять ID в .env легко и быстро.

---

#### ➡️ Регулярно (каждый день)
**Выбирайте:** `admin_approval`

Автоматизированный процесс экономит время.

---

#### ➡️ Постоянно (много новых пользователей)
**Выбирайте:** `open`

Или создайте собственную систему регистрации.

---

### Вопрос 3: Нужен ли контроль?

#### ➡️ Да, строгий контроль
**Выбирайте:** `whitelist` или `admin_approval`

Вы знаете всех пользователей и контролируете доступ.

---

#### ➡️ Нет, доверяем всем
**Выбирайте:** `open`

---

#### ➡️ Временно закрыть доступ
**Выбирайте:** `closed`

Потом можно вернуться к другому режиму.

---

## 📊 Сравнительная таблица

| Критерий | open | whitelist | admin_approval | closed |
|----------|------|-----------|----------------|--------|
| Простота настройки | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Контроль доступа | ❌ | ✅ | ✅ | ✅✅ |
| Скорость выдачи | Мгновенно | Мгновенно | Ожидание | N/A |
| Масштабируемость | ✅✅ | ⚠️ | ✅ | ❌ |
| Работа админа | Минимум | Средне | Средне | Минимум |
| Безопасность | ⚠️ | ✅ | ✅ | ✅✅ |
| История | ❌ | ❌ | ✅ | ❌ |

---

## 💼 Примеры использования

### Пример 1: Стартап (5 человек)
```env
AUTH_MODE=whitelist
TELEGRAM_ADMIN_IDS=founder_id
ALLOWED_USER_IDS=founder_id,dev1_id,dev2_id,marketer_id,designer_id
```

---

### Пример 2: Средняя компания (50 человек)
```env
AUTH_MODE=admin_approval
TELEGRAM_ADMIN_IDS=cto_id,sysadmin_id
```

Новые сотрудники:
1. Получают инструкцию в onboarding
2. Отправляют `/request_access` боту
3. HR или IT одобряет: `/approve_<id>`

---

### Пример 3: Публичный VPN
```env
AUTH_MODE=open
TELEGRAM_ADMIN_IDS=owner_id
```

Рекламируете в соцсетях, любой может подключиться.

---

### Пример 4: Личный VPN
```env
AUTH_MODE=closed
TELEGRAM_ADMIN_IDS=my_id
```

Только вы используете через админские команды.

---

## 🔄 Миграция между режимами

### Из open → admin_approval
```bash
# 1. Измените режим
AUTH_MODE=admin_approval

# 2. Перезапустите
docker-compose restart tgbot

# 3. Текущие пользователи продолжат работать
# 4. Новые должны запросить доступ
```

---

### Из whitelist → admin_approval
```bash
# 1. Измените режим
AUTH_MODE=admin_approval

# 2. Импортируйте существующих (опционально)
# Можно вручную одобрить текущих пользователей через БД
# Или они просто запросят доступ заново

# 3. Перезапустите
docker-compose restart tgbot
```

---

## 🎯 Рекомендации

### Для максимальной безопасности:
```env
AUTH_MODE=admin_approval
# + регулярный аудит /approved
# + мониторинг /authstats
# + удаление неактивных пользователей
```

---

### Для максимального удобства:
```env
AUTH_MODE=open
# + мониторинг использования
# + ограничение на сервере (bandwidth, connections)
```

---

### Золотая середина:
```env
AUTH_MODE=admin_approval
# Баланс между безопасностью и удобством
```

---

## ❓ FAQ

**Q: Могу ли я изменить режим после запуска?**
A: Да, просто измените AUTH_MODE в .env и перезапустите бота.

**Q: Что будет с текущими пользователями при смене режима?**
A: Одобренные пользователи (в БД) сохраняются, но применяются новые правила доступа.

**Q: Как добавить второго админа?**
A: Добавьте его ID в TELEGRAM_ADMIN_IDS через запятую:
```env
TELEGRAM_ADMIN_IDS=123456789,987654321
```

**Q: Можно ли комбинировать whitelist и admin_approval?**
A: Нет, но в whitelist режиме можно вручную добавлять одобренных из admin_approval БД.

**Q: Как узнать Telegram ID?**
A: Отправьте /start боту @userinfobot или @raw_data_bot

---

## 🚀 Быстрый старт

### Для большинства случаев рекомендуем:

```env
# Выберите один из вариантов:

# Вариант А: Контролируемый доступ (рекомендуется)
AUTH_MODE=admin_approval

# Вариант Б: Открытый доступ
AUTH_MODE=open

# Вариант В: Строгий контроль
AUTH_MODE=whitelist
ALLOWED_USER_IDS=your_ids_here
```

Затем:
```bash
docker-compose restart tgbot
```

---

Нужна помощь? Читайте полную документацию: [AUTHORIZATION.md](AUTHORIZATION.md)
